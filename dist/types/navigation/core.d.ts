import * as React from "react";
declare type Props = {
  inner: React.ReactNode;
  children: React.ReactNode;
  navbarOpen: boolean;
};
declare function Core(props: Props): React.ReactNode;
export default Core;
